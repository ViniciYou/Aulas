#include <stdio.h>

int main() {


    //Verifica se o valor é par ou impar
    /*int x;
    printf("Digite um valor inteiro: \n");
    scanf("%d", &x);

    //operador ternario

    //condição ? valor se verdadeiro : valor se falso;
    x % 2 == 0 ? printf("par\n") : printf("impar\n");
    */

    /*-----------------------------------------------------------------*/

    /*char produtividade;
    double salarioAtual, salarioReajustado;

    //Programa:
    //se o ususario cumpriu a produtividade
    //aumenta-se 10% se não 5% no seu salario.

    fflush(stdin);
    printf("Digite 's' ou 'S' para produtividade, se atingiu.\n");
    produtividade = getchar();
    fflush(stdin);
    printf("Digite o salario atual:\n");
    scanf("%lf",&salarioAtual);
    salarioReajustado = produtividade == 's' || produtividade == 'S' ? salarioAtual * 1.1 : salarioAtual * 1.05;

    printf("Novo salario: %.2lf \n", salarioReajustado);
    */

    /*----------------------------------------------------------------------------------------------------------------*/

    //Dias da semana usando switch
    /*int valor;

    printf("Digite um valor de um a sete: \n");
    scanf("%d", &valor);
    switch (valor) {
        case 1:
            printf("Domingo!");
            break;
        case 2:
            printf("Segunda-Feira!");
            break;
        case 3:
            printf("Terca-Feira!");
            break;
        case 4:
                printf("Quarta-Feira!");
            break;
        case 5:
            printf("Quinta-Feira!");
            break;
        case 6:
            printf("Sexta-Feira!");
            break;
        case 7:
            printf("Sabado!");
            break;
        default:
            printf("Opcao Invalida!");
    }
     */

    /*---------------------------------------------------------------------------------------------------*/


    //descobrir se letra e vogal usando switch
    /*char letra;
    printf("Digite uma letra minuscula: \n");
    letra = getchar();
    switch (letra) {
        case 'a':
        case 'e':
        case 'i':
        case 'o':
        case 'u':
            printf("E vogal \n");
            break;
        default:
            printf("Nao e vogal!\n");
    }
    */
    return 0;
}
